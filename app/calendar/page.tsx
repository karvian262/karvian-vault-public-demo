"use client";import AppShell from "@/components/AppShell";import { PageTitle } from "@/components/UI";import { useVault } from "@/lib/store";export default function Calendar(){const v=useVault();return <AppShell><PageTitle title="Calendar" subtitle="Deadlines from projects and tasks."/><div className="grid gap-4">{[...v.state.projects.map(p=>({title:p.name,date:p.deadline,type:'Project'})),...v.state.tasks.map(t=>({title:t.title,date:t.due,type:'Task'}))].sort((a,b)=>a.date.localeCompare(b.date)).map((x,i)=><div className="card p-5" key={i}><span className="badge">{x.type}</span><h2 className="mt-3 text-xl font-black">{x.title}</h2><p className="muted">{x.date}</p></div>)}</div></AppShell>}
