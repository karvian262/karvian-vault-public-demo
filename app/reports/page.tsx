"use client";import AppShell from "@/components/AppShell";import { PageTitle, StatCard } from "@/components/UI";import { useVault } from "@/lib/store";export default function Reports(){const v=useVault();const done=v.state.tasks.filter(t=>t.status==='done').length;return <AppShell><PageTitle title="Reports" subtitle="Founder progress report."/><div className="grid gap-4 md:grid-cols-4"><StatCard label="Projects" value={v.stats.projects}/><StatCard label="Open Tasks" value={v.stats.tasks}/><StatCard label="Done Tasks" value={done}/><StatCard label="Saved Knowledge" value={v.stats.chats+v.stats.notes}/></div><div className="card mt-6 p-6"><h2 className="text-2xl font-black">Weekly Brief</h2><p className="muted mt-3">Focus on critical tasks first. KARVIAN OS and Karvian Vault are the strongest active priorities.</p></div></AppShell>}
